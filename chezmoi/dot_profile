# ~/.profile — legacy redirect for non-zsh shells
[[ -r ~/.zprofile ]] && . ~/.zprofile
# Added by LM Studio CLI (lms)
export PATH="$PATH:/Users/jason/.cache/lm-studio/bin"
# End of LM Studio CLI section

